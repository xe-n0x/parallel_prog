# Лабораторная работа №2

## Тема

Параллельное умножение двух квадратных матриц на языке C++ с использованием OpenMP.

## Цель работы

Реализовать параллельную версию программы для умножения квадратных матриц и сравнить её время работы с последовательной реализацией из лабораторной работы №1.

## Задание

Необходимо написать программу на языке C/C++ для перемножения двух квадратных матриц с использованием технологии OpenMP.

**Исходные данные:** файлы, содержащие значения исходных матриц.

**Выходные данные:** файл со значениями результирующей матрицы, временем выполнения и объёмом задачи.

**Дополнительное требование:** выполнить автоматизированную проверку результата вычислений с помощью сторонней библиотеки или стороннего программного обеспечения.

## Используемые файлы проекта

| Файл | Назначение |
|---|---|
| `lab2_pp.cpp` | Основная C++-программа: чтение матриц из файлов, параллельное умножение с использованием OpenMP, измерение времени выполнения и запись результата. |
| `generate_matrix.py` | Python-скрипт для генерации квадратных матриц заданного размера. |
| `eqw.py` | Python-скрипт для автоматической проверки результата с использованием библиотеки NumPy. |
| `matrixA.txt` | Файл с первой исходной матрицей. |
| `matrixB.txt` | Файл со второй исходной матрицей. |
| `matrixC.txt` | Файл с результирующей матрицей, полученной C++-программой. |
| `matrixAB.txt` | Итоговый файл с результатом вычислений, временем выполнения и статусом проверки. |

## Формат входных данных

Входные матрицы хранятся в текстовых файлах `matrixA.txt` и `matrixB.txt`.

Матрицы являются квадратными и имеют размер `N × N`.

## Описание алгоритма

В лабораторной работе используется классический алгоритм умножения матриц, но вычисления распределяются между потоками OpenMP.

Пусть заданы две квадратные матрицы:

- `A` размера `N × N`;
- `B` размера `N × N`.

Результирующая матрица `C` также имеет размер `N × N`.

Каждый элемент результирующей матрицы вычисляется по формуле:

```text
C[i][j] = A[i][0] * B[0][j] + A[i][1] * B[1][j] + ... + A[i][N-1] * B[N-1][j]
```

В общем виде:

```text
C[i][j] = sum(A[i][k] * B[k][j]), где k = 0 ... N - 1
```

В последовательной версии все элементы матрицы вычислялись одним потоком. В параллельной версии часть итераций внешнего цикла распределяется между несколькими потоками:

```cpp
#pragma omp parallel for
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        for (int k = 0; k < p; k++) {
            C[i][j] += A[i][k] * B[k][j];
        }
    }
}
```

В тестировании использовалось `16` потоков.

## Теоретическая сложность

Сам алгоритм умножения матриц не изменился: для каждой пары индексов `(i, j)` выполняется проход по индексу `k`.

Для квадратных матриц размера `N × N` количество итераций внутреннего цикла равно:

```text
N^3
```

Асимптотическая сложность алгоритма остаётся:

```text
O(N^3)
```

При этом OpenMP позволяет выполнять часть вычислений одновременно на нескольких потоках, поэтому фактическое время выполнения становится меньше, чем у последовательной версии.

## Автоматизированная проверка результата

Проверка результата выполняется с помощью Python-скрипта `eqw.py`.

Скрипт:

1. считывает исходные матрицы `matrixA.txt` и `matrixB.txt`;
2. считывает результат из `matrixC.txt`;
3. вычисляет контрольный результат с помощью функции `numpy.dot`;
4. сравнивает результат C++-программы с результатом NumPy.

Если результат совпадает, в итоговом файле фиксируется статус:

```text
Correctly: 1
```

## Результаты тестирования OpenMP-версии

Для оценки времени работы параллельного алгоритма были выполнены тесты на квадратных матрицах разных размеров.

Фактические замеры были получены для `4`, `8`, `16` потоков OpenMP.


### Результаты для 16 потоков

| № теста | Размер матриц | Количество потоков | Время выполнения | Время выполнения, с | Проверка результата |
|---:|---:|---:|---:|---:|---:|
| 1 | `200 × 200` | `16` | `39 ms` | `0.039 s` | `Correctly: 1` |
| 2 | `400 × 400` | `16` | `202 ms` | `0.202 s` | `Correctly: 1` |
| 3 | `800 × 800` | `16` | `1 424 ms` | `1.424 s` | `Correctly: 1` |
| 4 | `1600 × 1600` | `16` | `11 961 ms` | `11.961 s` | `Correctly: 1` |

Фрагменты итоговых результатов тестирования:

```text
200 x 200:   Time: 39 ms, Correctly: 1
400 x 400:   Time: 202 ms, Correctly: 1
800 x 800:   Time: 1424 ms, Correctly: 1
1600 x 1600: Time: 11961 ms, Correctly: 1
```

### Данные для 4, 8 и 16 потоков

| Размер матриц | Потоки | Время выполнения | Время выполнения, с | Ускорение относительно ЛР №1 | Снижение времени |
|---:|---:|---:|---:|---:|---:|---|
| `200 × 200` | `4` | `55 ms` | `0.055 s` | `2.16` | `53.8%` |
| `200 × 200` | `8` | `44 ms` | `0.044 s` | `2.68` | `62.7%` |
| `200 × 200` | `16` | `39 ms` | `0.039 s` | `3.05` | `67.2%` |
| `400 × 400` | `4` | `311 ms` | `0.311 s` | `2.41` | `58.4%` |
| `400 × 400` | `8` | `238 ms` | `0.238 s` | `3.14` | `68.2%` |
| `400 × 400` | `16` | `202 ms` | `0.202 s` | `3.71` | `73.0%` |
| `800 × 800` | `4` | `2 224 ms` | `2.224 s` | `2.44` | `59.0%` |
| `800 × 800` | `8` | `1 691 ms` | `1.691 s` | `3.21` | `68.8%` |
| `800 × 800` | `16` | `1 424 ms` | `1.424 s` | `3.81` | `73.7%` |
| `1600 × 1600` | `4` | `18 322 ms` | `18.322 s` | `2.39` | `58.1%` |
| `1600 × 1600` | `8` | `14 081 ms` | `14.081 s` | `3.11` | `67.8%` |
| `1600 × 1600` | `16` | `11 961 ms` | `11.961 s` | `3.66` | `72.7%` |

### Сравнение 4, 8 и 16 потоков

| Размер матриц | 4 потока | 8 потоков | 16 потоков | Ускорение 8 потоков относительно 4 | Ускорение 16 потоков относительно 8 |
|---:|---:|---:|---:|---:|---:|
| `200 × 200` | `55 ms` | `44 ms` | `39 ms` | `1.25` | `1.13` |
| `400 × 400` | `311 ms` | `238 ms` | `202 ms` | `1.31` | `1.18` |
| `800 × 800` | `2 224 ms` | `1 691 ms` | `1 424 ms` | `1.32` | `1.19` |
| `1600 × 1600` | `18 322 ms` | `14 081 ms` | `11 961 ms` | `1.30` | `1.18` |

Из таблицы видно, что переход с `4` на `8` потоков даёт более заметное сокращение времени, чем переход с `8` на `16` потоков. Это связано с тем, что при увеличении числа потоков возрастает влияние накладных расходов OpenMP, доступа к памяти и непараллелизуемой части программы.

## Сравнение с лабораторной работой №1

В лабораторной работе №1 использовалась последовательная версия программы. Во второй лабораторной работе используется параллельная версия на OpenMP.

Ниже приведено сравнение последовательной версии с OpenMP-версией на `4`, `8` и `16` потоках.

| Размер матриц | ЛР №1, последовательная версия | OpenMP 4 потока | OpenMP 8 потоков | OpenMP 16 потоков |
|---:|---:|---:|---:|---:|
| `200 × 200` | `119 ms` | `55 ms` | `44 ms` | `39 ms` |
| `400 × 400` | `749 ms` | `311 ms` | `238 ms` | `202 ms` |
| `800 × 800` | `5 422 ms` | `2 224 ms` | `1 691 ms` | `1 424 ms` |
| `1600 × 1600` | `43 765 ms` | `18 322 ms` | `14 081 ms` | `11 961 ms` |

### Ускорение относительно последовательной версии

| Размер матриц | OpenMP 4 потока | OpenMP 8 потоков | OpenMP 16 потоков |
|---:|---:|---:|---:|
| `200 × 200` | `2.16` | `2.68` | `3.05` |
| `400 × 400` | `2.41` | `3.14` | `3.71` |
| `800 × 800` | `2.44` | `3.21` | `3.81` |
| `1600 × 1600` | `2.39` | `3.11` | `3.66` |


## Анализ результатов

По результатам тестирования видно, что OpenMP-версия работает быстрее последовательной реализации на всех размерах матриц.

## Вывод

В ходе лабораторной работы была разработана параллельная версия программы для умножения двух квадратных матриц на языке C++ с использованием OpenMP.

Программа считывает исходные матрицы из файлов, выполняет умножение, сохраняет результирующую матрицу и фиксирует время выполнения. Корректность результата проверяется с помощью сторонней библиотеки NumPy.

По сравнению с последовательной реализацией из лабораторной работы №1, OpenMP-версия показала заметное уменьшение времени выполнения. Для `16` потоков фактическое ускорение на тестовых размерах `200 × 200`, `400 × 400`, `800 × 800` и `1600 × 1600` составило примерно от `3.05` до `3.81` раза.

Также были добавлены данные для `4` и `8` потоков. Они показывают, что увеличение числа потоков ускоряет выполнение программы, но прирост постепенно снижается. Наиболее заметное улучшение наблюдается при переходе от последовательной версии к `4` потокам и от `4` к `8` потокам. Переход от `8` к `16` потокам тоже уменьшает время выполнения, но уже не так значительно.

Полученные результаты показывают, что параллельное выполнение позволяет ускорить классический алгоритм умножения матриц, особенно на больших размерах входных данных. При этом эффективность использования каждого отдельного потока снижается при увеличении общего числа потоков, что является нормальным для параллельных программ из-за накладных расходов и ограничений памяти.
